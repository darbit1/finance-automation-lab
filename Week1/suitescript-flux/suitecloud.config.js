/**
 * SuiteCloud CLI project configuration. `src` holds the SDF account-customization project
 * (FileCabinet scripts + Objects). Used by `suitecloud` commands and by the Jest unit tests.
 */
module.exports = {
  defaultProjectFolder: 'src',
  commands: {}
};
