{
  "name": "suitescript-flux",
  "version": "1.0.0",
  "private": true,
  "description": "NetSuite-native flux review: deterministic SuiteQL/SuiteScript calc + embedded N/llm narrative + a ported number/provenance audit seam.",
  "scripts": {
    "test": "jest",
    "deploy": "suitecloud project:deploy",
    "validate": "suitecloud project:validate --server"
  },
  "devDependencies": {
    "@oracle/suitecloud-cli": "^3.0.0",
    "@oracle/suitecloud-unit-testing": "^1.5.0",
    "jest": "^29.7.0"
  }
}
